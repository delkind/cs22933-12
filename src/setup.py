from setuptools import setup

setup(
   name='tagger',
   version='1.0',
   description='part-of-speech tagger project using Hidden Markov Model',
   author='David Elkind, Assaf Lehr',
   author_email='hidden_email',
   #packages=['tagger'],  #same as name
   install_requires=['numpy', ]
)